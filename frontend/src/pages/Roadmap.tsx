import { useEffect, useState } from 'react'
import { Check, Circle, Clock3, Sparkles } from 'lucide-react'
import { api } from '../services/api'
import type { RoadmapItem, StudentProfile } from '../types'
import SectionHeader from '../components/SectionHeader'

export default function Roadmap({student}:{student:StudentProfile}){const [items,setItems]=useState<RoadmapItem[]>([]);const [error,setError]=useState('');useEffect(()=>{api.roadmap().then(setItems).catch(err=>setError(err instanceof Error?err.message:'Could not load roadmap.'))},[]);const complete=async(item:RoadmapItem)=>{setError('');try{const x=await api.completeRoadmapItem(item.id);setItems(current=>current.map(i=>i.id===item.id?x:i))}catch(err){setError(err instanceof Error?err.message:'Could not update this milestone.')}};if(error && !items.length)return <div className="loading-box">{error}</div>;return <div><div className="page-hero"><div><span className="eyebrow">Class {student.class_level} roadmap</span><h1>My Roadmap</h1><p>A time-bound set of next steps. Not a rigid life plan.</p></div></div>{error && items.length > 0 && <div className="loading-box">{error}</div>}<div className="roadmap-header"><div><Sparkles size={17}/><span>Built around your current profile</span></div><strong>{items.filter(x=>x.completed).length} / {items.length} complete</strong></div><SectionHeader eyebrow="Your journey" title="What to do next"/><div className="roadmap-list">{items.map((item,idx)=><div className={`roadmap-item ${item.completed?'completed':''}`} key={item.id}><div className="roadmap-marker">{item.completed?<div className="done-marker"><Check size={15}/></div>:<Circle size={13}/>}</div><div className="roadmap-copy"><div className="roadmap-item-head"><span className="horizon">{item.horizon}</span><span className="item-type">{item.type}</span></div><h3>{item.title}</h3><p>{item.description}</p>{!item.completed&&<button className="secondary-btn small" onClick={()=>complete(item)}><Check size={15}/> Mark complete</button>}</div><div className="roadmap-time"><Clock3 size={14}/> {idx<2?'This month':idx<4?'Next 3 months':'Later'}</div></div>)}</div></div>}
